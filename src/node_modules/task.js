const task = (target, name, descriptor) => {
  const { value: fn } = descriptor
  function decorated () {


    const listeners = []
    const promise = wrapCall(fn,this, arguments)
    promise.onProgress = listener => {}
  }
  return { ...descriptor, value: decorated }
}
export { task as default }
